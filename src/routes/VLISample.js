import React from "react";
import AppLayout from "../components/AppLayout";
import VersionListItem from "../features/VersionListItem/index";

const VLISample = () => {
	return (
		<AppLayout>
			<VersionListItem />
		</AppLayout>
	);
};

export default VLISample;
